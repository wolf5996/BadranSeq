% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/BadranSeq.R
\name{do_FeaturePlot}
\alias{do_FeaturePlot}
\title{Enhanced Feature Plot with Viridis Support}
\usage{
do_FeaturePlot(
  object,
  features,
  reduction = "umap",
  dims = c(1, 2),
  slot = "data",
  use_viridis = TRUE,
  viridis.palette = "B",
  viridis.direction = -1,
  order = TRUE,
  pt.size = 1,
  pt.alpha = 1,
  plot.axes = TRUE,
  variance_digits = 1,
  min.cutoff = NA,
  max.cutoff = NA,
  na.value = "grey90",
  split.by = NULL,
  plot_cell_borders = TRUE,
  border.size = 2,
  border.color = "black",
  ...
)
}
\arguments{
\item{object}{Seurat object.}

\item{features}{character. Gene names or metadata columns to plot.}

\item{reduction}{character. Reduction to use (default: "umap").}

\item{dims}{numeric. Vector of 2 PC dimensions to plot (default: c(1, 2)).}

\item{slot}{character. Data slot ("data", "counts", "scale.data"). Default: "data".}

\item{use_viridis}{logical. Use viridis color scale (default: TRUE).}

\item{viridis.palette}{character. Viridis palette "A"-"H" (default: "B").}

\item{viridis.direction}{numeric. Color direction -1 or 1 (default: -1).}

\item{order}{logical. Plot high-expressing cells on top (default: TRUE).}

\item{pt.size}{numeric. Point size (default: 1).}

\item{pt.alpha}{numeric. Point transparency (default: 1).}

\item{plot.axes}{logical. Show axes (default: TRUE).}

\item{variance_digits}{numeric. Decimal places for variance (default: 1).}

\item{min.cutoff}{numeric or "q10". Minimum expression cutoff.}

\item{max.cutoff}{numeric or "q90". Maximum expression cutoff.}

\item{na.value}{character. Color for NA/zero values (default: "grey90").}

\item{split.by}{character. Metadata column for splitting (creates panels with silhouettes).}

\item{plot_cell_borders}{logical. Plot black borders around cells (default: TRUE).}

\item{border.size}{numeric. Border size multiplier (default: 2).}

\item{border.color}{character. Border color (default: "black").}

\item{...}{Additional arguments (currently unused).}
}
\value{
ggplot2 object or list of ggplot2 objects for multiple features.
}
\description{
Creates feature expression plots overlaid on dimensionality reductions
with viridis color scales and publication-ready styling.
}
\examples{
\dontrun{
do_FeaturePlot(seurat_object, features = "CD3D")
do_FeaturePlot(seurat_object, features = c("CD3D", "CD8A", "CD4"))
do_FeaturePlot(seurat_object, features = "CD3D", reduction = "pca")
}

}
