% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gsea-plot.R
\name{do_GseaPlot}
\alias{do_GseaPlot}
\title{Publication-Ready GSEA Results Barplot}
\usage{
do_GseaPlot(
  gsea_object,
  analysis_name = "GSEA Analysis",
  simplify_go = TRUE,
  number_of_pathways = 10,
  selection = NULL,
  fill_scale = c("diverging", "viridis"),
  show_padj = TRUE,
  show_leading_edge = TRUE,
  title = NULL
)
}
\arguments{
\item{gsea_object}{A \code{gseaResult} object from \code{clusterProfiler::gseGO()},
\code{ReactomePA::gsePathway()}, or similar, containing results in the
\verb{@result} slot.}

\item{analysis_name}{Character. Subtitle for the plot. Defaults to
\code{"GSEA Analysis"}.}

\item{simplify_go}{Logical. Whether to apply \code{clusterProfiler::simplify()}
to GO terms (default: \code{TRUE}). Only applies when GO term IDs are
detected; silently skipped otherwise.}

\item{number_of_pathways}{Integer. Number of top upregulated and
downregulated pathways to display (default: \code{10}). Ignored when
\code{selection} is provided.}

\item{selection}{Character vector of pathway descriptions to plot.
Overrides automatic top/bottom selection. Defaults to \code{NULL}.}

\item{fill_scale}{Character. Color scale: \code{"diverging"} (RdBu, 0-centered)
or \code{"viridis"} (sequential viridis-C, original style). Defaults to
\code{"diverging"} to emphasize the signed nature of NES.}

\item{show_padj}{Logical. Show adjusted p-value labels on bars.
Defaults to \code{TRUE}.}

\item{show_leading_edge}{Logical. Show leading-edge gene count and
gene symbols in y-axis labels. Defaults to \code{TRUE}.}

\item{title}{Character. Custom plot title. If \code{NULL}, auto-generated
from \code{analysis_name}.}
}
\value{
A ggplot2 object.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#stable}{\figure{lifecycle-stable.svg}{options: alt='[Stable]'}}}{\strong{[Stable]}}

Creates a horizontal barplot of top pathways from a Gene Set Enrichment
Analysis (GSEA) result. Instead of the default dotplot, this function
shows Normalized Enrichment Scores (NES) as bars with adjusted p-values
as text labels and leading-edge gene annotations.
}
\examples{
\dontrun{
data(gsea_pbmc3k)
do_GseaPlot(gsea_pbmc3k, analysis_name = "CD14+ Mono vs Others")

# Use viridis (original style)
do_GseaPlot(gsea_pbmc3k, fill_scale = "viridis")

# Plot specific pathways
do_GseaPlot(
  gsea_pbmc3k,
  selection = c("cell chemotaxis")
)
}

}
