% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fetch.R
\name{fetch_feature_data}
\alias{fetch_feature_data}
\title{Fetch Feature Expression Data from a Seurat Object}
\usage{
fetch_feature_data(
  object,
  features,
  layer = "data",
  assay = NULL,
  reductions = NULL,
  dims = 1:2,
  metadata = TRUE
)
}
\arguments{
\item{object}{Seurat object.}

\item{features}{character. Gene names to extract (required). Becomes a factor
column preserving the input order.}

\item{layer}{character. Layer(s) to extract expression from.
One of "counts", "data", "scale.data", or a combination
(default: "data").}

\item{assay}{character. Assay to use (default: NULL = DefaultAssay).}

\item{reductions}{character. Reduction names to include (default: NULL = none).}

\item{dims}{integer. Dimensions to extract per reduction (default: 1:2).}

\item{metadata}{logical or character. TRUE = all metadata columns,
FALSE = none, or a character vector of specific column names.}
}
\value{
A tibble with columns: cell_id, features (factor), one column per layer,
optional embedding columns, optional metadata columns.
}
\description{
Extracts a tidy tibble with one row per cell per feature. Expression values
are provided as one column per requested layer. Optionally includes
embedding coordinates and metadata, joined by cell_id.
}
\examples{
\dontrun{
# Basic usage -- normalized expression
fetch_feature_data(seurat_obj, features = c("CD3D", "CD8A"))

# Multiple layers
fetch_feature_data(seurat_obj, features = "CD3D",
                   layer = c("counts", "data"))

# With embeddings and specific metadata
fetch_feature_data(seurat_obj, features = c("CD3D", "CD14"),
                   reductions = "umap",
                   metadata = c("seurat_clusters", "condition"))

# Explicit assay
fetch_feature_data(seurat_obj, features = "CD3D",
                   assay = "RNA", layer = "counts")
}

}
