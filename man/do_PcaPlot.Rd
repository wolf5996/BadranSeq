% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scRNA_Seq_functions.R
\name{do_PcaPlot}
\alias{do_PcaPlot}
\title{Enhanced PCA Plot with Variance Explained}
\usage{
do_PcaPlot(
  sample,
  dims = c(1, 2),
  group.by = NULL,
  split.by = NULL,
  colors.use = NULL,
  variance_digits = 1,
  ...
)
}
\arguments{
\item{sample}{Seurat object, generated by CreateSeuratObject.}

\item{dims}{numeric. Vector of 2 numerics indicating the PC dimensions to plot.
Defaults to c(1, 2).}

\item{group.by}{character. Metadata variable to group the output by.
Has to be a character or factor column.}

\item{split.by}{character. Secondary metadata variable to further group (split)
the output by. Has to be a character or factor column.}

\item{colors.use}{named_vector. Named vector of valid color representations
with as many named colors as unique values of group.by.}

\item{variance_digits}{numeric. Number of decimal places for variance percentages.
Defaults to 1.}

\item{...}{Additional arguments passed to \code{SCpubr::do_DimPlot()}.}
}
\value{
ggplot2 object with PCA plot including variance explained in axis labels.
}
\description{
A wrapper around \code{SCpubr::do_DimPlot()} specifically designed for PCA plots that
automatically adds variance explained percentages to the axis labels.
}
\examples{
\dontrun{
# Basic PCA plot with variance explained
do_PcaPlot(seurat_object, group.by = "condition")

# Plot specific PC dimensions
do_PcaPlot(seurat_object, dims = c(2, 3), group.by = "cluster")

# With custom colors
colors <- c("Control" = "#2E86AB", "Treatment" = "#A23B72")
do_PcaPlot(seurat_object, group.by = "condition", colors.use = colors)
}

}
