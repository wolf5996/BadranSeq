% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scRNA_Seq_functions.R
\name{do_UmapPlot}
\alias{do_UmapPlot}
\title{Enhanced UMAP Plot with Preferred Settings}
\usage{
do_UmapPlot(
  sample,
  dims = c(1, 2),
  group.by = NULL,
  split.by = NULL,
  colors.use = NULL,
  plot.axes = TRUE,
  label = TRUE,
  label.size = 4,
  label.color = "black",
  label.fill = "white",
  pt.size = 1,
  ...
)
}
\arguments{
\item{sample}{Seurat object, generated by CreateSeuratObject.}

\item{dims}{numeric. Vector of 2 numerics indicating the UMAP dimensions to plot.
Defaults to c(1, 2).}

\item{group.by}{character. Metadata variable to group the output by.
Has to be a character or factor column.}

\item{split.by}{character. Secondary metadata variable to further group (split)
the output by. Has to be a character or factor column.}

\item{colors.use}{named_vector. Named vector of valid color representations
with as many named colors as unique values of group.by.}

\item{plot.axes}{logical. Whether to plot axes. Defaults to TRUE.}

\item{label}{logical. Whether to plot cluster labels. Defaults to TRUE.}

\item{label.size}{numeric. Size of the labels. Defaults to 4.}

\item{label.color}{character. Color of the labels. Defaults to "black".}

\item{label.fill}{character. Color to fill the labels. Defaults to "white".}

\item{pt.size}{numeric. Size of the dots. Defaults to 1.}

\item{...}{Additional arguments passed to \code{SCpubr::do_DimPlot()}.}
}
\value{
ggplot2 object with UMAP plot using preferred settings.
}
\description{
A wrapper around \code{SCpubr::do_DimPlot()} specifically designed for UMAP plots with
optimized default settings for axes and labels.
}
\examples{
\dontrun{
# Basic UMAP plot with preferred settings
do_UmapPlot(seurat_object, group.by = "condition")

# With custom colors
colors <- c("Control" = "#2E86AB", "Treatment" = "#A23B72")
do_UmapPlot(seurat_object, group.by = "condition", colors.use = colors)

# Without labels
do_UmapPlot(seurat_object, group.by = "condition", label = FALSE)
}

}
