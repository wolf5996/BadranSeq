% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/BadranSeq.R
\name{do_DimPlot}
\alias{do_DimPlot}
\title{Generalized Dimensionality Reduction Plot}
\usage{
do_DimPlot(
  object,
  reduction = "umap",
  dims = c(1, 2),
  group.by = NULL,
  split.by = NULL,
  colors.use = NULL,
  label = TRUE,
  label.size = 4,
  label.color = "black",
  label.fill = "white",
  pt.size = 1,
  pt.alpha = 1,
  plot.axes = TRUE,
  variance_digits = 1,
  shuffle = TRUE,
  plot_cell_borders = TRUE,
  border.size = 2,
  border.color = "black",
  legend.title = NULL,
  show.legend = TRUE,
  ncol = NULL,
  ...
)
}
\arguments{
\item{object}{Seurat object.}

\item{reduction}{character. Reduction to use (default: "umap").}

\item{dims}{numeric. Vector of 2 PC dimensions to plot (default: c(1, 2)).}

\item{group.by}{character. One or more metadata columns for grouping.
If NULL (default), uses active Idents. When multiple columns are provided,
each gets its own panel combined via patchwork.}

\item{split.by}{character. Metadata column for splitting (creates panels with silhouettes).}

\item{colors.use}{Named vector of colors for groups.}

\item{label}{logical. Show cluster labels (default: TRUE).}

\item{label.size}{numeric. Label font size (default: 4).}

\item{label.color}{character. Label text color (default: "black").}

\item{label.fill}{character. Label background fill (default: "white").}

\item{pt.size}{numeric. Point size (default: 1).}

\item{pt.alpha}{numeric. Point transparency (default: 1).}

\item{plot.axes}{logical. Show axes (default: TRUE).}

\item{variance_digits}{numeric. Decimal places for variance (default: 1).}

\item{shuffle}{logical. Randomize cell plotting order (default: TRUE).}

\item{plot_cell_borders}{logical. Plot black borders around cells (default: TRUE).}

\item{border.size}{numeric. Border size multiplier (default: 2).}

\item{border.color}{character. Border color (default: "black").}

\item{legend.title}{character. Custom legend title (NULL removes title).}

\item{show.legend}{logical. Show the colour legend (default: TRUE). Set to FALSE to hide the legend entirely.}

\item{ncol}{numeric. Number of columns for split panels (default: NULL, auto).}

\item{...}{Additional arguments (currently unused).}
}
\value{
ggplot2 object with dimensionality reduction plot.
}
\description{
Unified wrapper for dimensionality reduction plots. Routes to specialized
functions for PCA and UMAP, handles other reductions directly. When split.by
is used, shows each split category with a grey silhouette of all other cells.
}
\examples{
\dontrun{
do_DimPlot(seurat_object, group.by = "condition")
do_DimPlot(seurat_object, reduction = "pca", group.by = "cluster")
do_DimPlot(seurat_object, split.by = "sample")  # Shows silhouettes
}

}
